// Array Destructuring

const colors = ["red", "blue", "green"];

const [first, second] = colors;

console.log(first);
console.log(second);

// Object Destructuring

const user = {
    name: "Shashank",
    age: 21
};

const { name, age } = user;

console.log(name);
console.log(age);